@AGENTS.md

## Claude-specific instructions

- Before starting any task, always read `.claude/skills/*` or `.claude/agents/*`
- After completing each module, run `pnpm lint` before moving to the next task
- When uncertain, always ask first — never make assumptions or arbitrary decisions
- Never run `pnpm build` during an agent session
